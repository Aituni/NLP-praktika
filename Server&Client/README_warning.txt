The servers currently only works with every file (except TEST files)
inside Tagger folder. And does not work concurrently, there are some cuda
errors when forking.

The server is only an experimental demo, it has no error control, and some
options still did not work.
